spring:
  application:
    name: anime-service
  threads:
    virtual:
      enabled: true

logging:
  level:
    animeservice: debug

server:
  servlet:
    context-path: /
  error:
    include-stacktrace: on-param
  tomcat:
    threads:
      max: 20
